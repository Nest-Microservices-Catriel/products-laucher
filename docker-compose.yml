# @format

version: '3.9'

services:
  nats-server:
    image: nats:latest
    ports:
      - '8222:8222'
      - '4222:4222'
      - '6222:6222'

  client-gateway:
    build: ./client-gateway
    ports:
      - '${CLIENT_GATEWAY_PORT}:3000'
    volumes:
      - ./client-gateway/src:/usr/source/app/src
    command: npm run start:dev
    environment:
      - PORT=3000
      - NATS_SERVERS=nats://nats-server:4222

  products-ms:
    build: ./products-ms
    volumes:
      - ./products-ms/src:/usr/source/app/src
    command: npm run start:dev
    environment:
      - PORT=3001
      - NATS_SERVERS=nats://nats-server:4222
      - DATABASE_URL=file:./dev.db

  orders-ms:
    depends_on:
      - orders-db
    build: ./orders-ms
    volumes:
      - ./orders-ms/src:/usr/source/app/src
    command: npm run start:dev
    environment:
      - PORT=3002
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=asda1234
      - POSTGRES_DB=ordersdb
      - POSTGRES_PORT=5432
      - POSTGRES_HOST=orders-db
      - NATS_SERVERS=nats://nats-server:4222

  orders-db:
    container_name: orders_database
    image: postgres:16.2
    restart: always
    ports:
      - 5432:5432
    volumes:
      - ./orders-ms/postgres:/var/lib/postgresql/data
    environment:
      - name=value
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=asda1234
      - POSTGRES_DB=ordersdb
